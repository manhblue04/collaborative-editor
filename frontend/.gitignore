node_modules/
dist/
*.log

# Env files — chứa secrets/config, KHÔNG commit
.env
.env.development
.env.production
.env.local
.env.*.local

# .env.example được COMMIT (là template, không chứa secrets)
!.env.example
